language: node_js
node_js:
  - "12.22.3"
  services: 
  - postgresql
  env:
   - DATABASE_URL=postgresql://localhost:5432/codexdb
   before_script:
  - psql -c 'create database codexdb;' -codex
  - psql -U codex -d codexdb -a -f usernames.sql


